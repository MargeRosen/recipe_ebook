<%= title(@recipe.title, "Recipes") %>
<%= link_to "Back to Recipe List", recipes_path(@recipe)%>
<br></br>
  <h2><%= @recipe.title %></h2>
  <%= link_to "Edit Recipe ", edit_recipe_path(@recipe)%>
       |
  <%= link_to "Delete Recipe ", recipe_path(@recipe),
      :method => :delete,
      data: { confirm: 'Are you sure you want to delete this recipe?' }%>
  <p><%= @recipe.description %></p>
  <p>Prep time:  <%=@recipe.preptime %></p>
  <p>Serves:  <%=@recipe.servings %></p>
  <ul id='instructions'>
    <% @recipe.instructions.each do |instruction| %>
      <li>Ingredients: <%=instruction.ingredient %></li>
      <li>Directions: <%=instruction.directions %></li>
  </ul>
    <% end %>
  <h4><%= link_to "New Instruction", new_recipe_instruction_path(@recipe) %></h4>

